# datasource 설정
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.datasource.url=jdbc:mariadb://localhost:3307/goodjobs
spring.datasource.username=goodjobs
spring.datasource.password=yeh1206

# 로그 설정
logging.level.org.springframework=info
logging.level.com.portfolio=debug

# jpa 설정
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true

# 첨부파일 업로드 설정
spring.servlet.multipart.enabled=true
spring.servlet.multipart.location=D:\\Portfolio\\GoodJobs\\Upload
spring.servlet.multipart.max-request-size=30MB
spring.servlet.multipart.max-file-size=10MB

com.portfolio.goodjobs.temp.path=${spring.servlet.multipart.location}\\Temp
com.portfolio.goodjobs.logo.path=${spring.servlet.multipart.location}\\Logo
